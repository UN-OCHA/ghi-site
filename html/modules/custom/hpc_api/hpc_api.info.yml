name: HPC API
description: Retrieve data from the HPC API.
package: HPC
configure: hpc_api.config

type: module
core_version_requirement: ^10.3 || ^11 || ^12

dependencies:
  - hpc_remote_data_cache:hpc_remote_data_cache
  - migrate_plus:migrate_plus
