(function ($, Drupal) {
  Drupal.behaviors.GhiMainMenu = {
    attach: function (context, settings) {
      // Drupal.behaviors.cdDropdown.updateDrupalTogglableMenus('.cd-nav > .menu > .menu-item > a + .menu-link-contentmain');
    }
  };
}(jQuery, Drupal));