uuid: 75a770c1-a6ab-4315-b8aa-fedb015f02de
langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_plan_is_global
    - node.type.plan
id: node.plan.field_plan_is_global
field_name: field_plan_is_global
entity_type: node
bundle: plan
label: 'Is global plan'
description: 'Check this if this plan should be treated as a global plan. The effect of this will be that data retrieval and display is done differently to normal HRPs in different parts of the site, mainly the summary and data pages of this plan. This will only take effect if the <em>Cluster filter</em> field is not empty and if there are at least 2 plans linked to this plan in the <em>Link plans</em> field below.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  on_label: ''
  off_label: ''
field_type: boolean
